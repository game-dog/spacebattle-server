#include <iostream>
#include "BitStream.h"

using namespace std;

int main(int argc, char* argv[]) {
	//OutputBitStream obs;

	//obs.WriteBits(13, 5);
	//obs.WriteBits(52, 6);
	//obs.WriteBits(47, 6);

	//InputBitStream ibs((uint8_t*)obs.GetBufferPtr(), 17);
	//uint8_t a, b, c;

	//ibs.ReadBits(a, 5);
	//ibs.ReadBits(b, 6);
	//ibs.ReadBits(c, 6);

	//cout << (int)a << ' ' << (int)b << ' ' << (int)c << '\n';
	//cout << ibs.GetRemainingBitCount() << '\n';
	
	return 0;
}